Dirty hack to try and determine the maximal sizes of the MEOS sub-component
libraries, and then plot them in a maybe useful way. Runs as part of the
normal test system - logs for regression tests will provide paths to
relevant .svg files.

